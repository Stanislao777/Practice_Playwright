{
  // Visit https://aka.ms/tsconfig to read more about this file
  "compilerOptions": {
    // File Layout
    // "rootDir": "./src",
    // "outDir": "./dist",

    // Environment Settings
    // See also https://aka.ms/tsconfig/module
    
    // For nodejs:
    // "lib": ["esnext"],
    // "types": ["node"],
    // and npm install -D @types/node

    // Other Outputs
    
    // Stricter Typechecking Options
    
    // Style Options
    // "noImplicitReturns": true,
    // "noImplicitOverride": true,
    // "noUnusedLocals": true,
    // "noUnusedParameters": true,
    // "noFallthroughCasesInSwitch": true,
    // "noPropertyAccessFromIndexSignature": true,

    // Recommended Options
    "target": "ESNext",                      // Usa la última versión de JavaScript
    "module": "CommonJS",                    // Compatible con Node.js
    "strict": true,                          // Activa comprobaciones estrictas
    "esModuleInterop": true,                 // Permite importar módulos CommonJS como ESModules
    "moduleResolution": "node",              // Resuelve módulos como lo hace Node.js
    "outDir": "dist",                        // Carpeta donde se guardan los .js compilados
    "rootDir": "src",                        // Carpeta donde están tus archivos .ts
    "types": ["@playwright/test"],           // Incluye los tipos de Playwright para autocompletado
    "resolveJsonModule": true,               // Permite importar archivos .json
    "skipLibCheck": true,                    // Evita errores en archivos de definición externos
  },
  "include": ["src", "playwright.config.ts"],			// Le dice a TypeScript que compile todos los archivos .ts dentro de la carpeta tests/ y también el archivo playwright.config.ts.
  "exclude": ["node_modules", "dist"]				    	// Ignora carpetas que no necesitas compilar, como dependencias (node_modules) y reportes  de pruebas.
}
